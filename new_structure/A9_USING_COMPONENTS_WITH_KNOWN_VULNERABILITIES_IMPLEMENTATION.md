# A9: Using Components with Known Vulnerabilities - Implementation Plan

## Objectives

Remediate risks identified in the assessment by enforcing deterministic dependency versions for production-critical packages, introducing automated auditing, and preventing regression of loose specifications.

## Actions Performed / To Perform

1. Pin versions for: `cryptography`, `PyMySQL`, `mysql-connector-python`.
2. Add audit script `scripts/dependency_audit.py` supporting optional `pip-audit` integration.
3. Add enforcement test to fail if future `>=` (or missing spec) sneaks into production dependency lines.
4. Document change and future improvements.

## Version Selection Rationale

- cryptography: Choose latest stable minor within current major to capture security patches while avoiding unexpected breaking changes.
- PyMySQL / mysql-connector-python: Pin to current widely adopted stable releases (driver stability critical for data integrity and auth reliability).

## Enforcement Test Rules

- Ignore lines under "Development and Testing" section for leniency.
- Permit version ranges ONLY for explicitly documented dev tooling (e.g., coverage).
- Disallow unpinned operators: `>=`, `>`, `<` (unless coverage exception) in production sections.

## Audit Script Behavior

- Lists all dependencies with classification (prod/dev).
- If `pip-audit` installed, runs vulnerability scan and prints summary; exits non-zero on vulnerabilities unless `--allow-vuln` provided.

## Deferred Enhancements

- Add SBOM generation (CycloneDX) pipeline step.
- Integrate dependency scanning into CI (GitHub Actions / GitLab CI).
- Add license compliance checking.

## Success Criteria

- requirements.txt updated with pins.
- tests/test_dependency_pinning.py passes.
- `python scripts/dependency_audit.py --summary` runs without error.

---

(Autogenerated planning document)
